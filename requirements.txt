customtkinter>=5.2
Pillow>=10.0
pyperclip>=1.8
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
# Backend partagé Jordan / Thomas (CRM + drafts + Convoi en cloud)
supabase>=2.4
# Fait que Python utilise le store CA Windows (sinon le MITM antivirus/proxy
# casse le TLS de httpx/supabase → SSL CERTIFICATE_VERIFY_FAILED au login).
# OBLIGATOIRE sur Windows. Sans ce paquet, impossible de se connecter à
# Supabase depuis l'app.
pip-system-certs>=4.0; sys_platform == "win32"
# Le Convoi — extraction de fichiers (toutes optionnelles, fallback gracieux)
openpyxl>=3.1     # Excel .xlsx / .xlsm
python-docx>=1.1  # Word .docx
pypdf>=4.0        # PDF
# OCR images : nécessite Tesseract OCR installé sur la machine
# pytesseract>=0.3
# Drag & drop (optionnel — sinon « Parcourir… » uniquement)
# tkinterdnd2>=0.3
# Le Phare — Google Search Console (optionnel ; sans ces 3 paquets,
# l'intégration GSC est désactivée gracieusement, le reste tourne).
# google-auth-oauthlib est requis pour le flow OAuth2 user (cf.
# scripts/phare_setup_gsc_oauth.py — seul moyen d'accéder aux Domain
# properties Search Console, le service account ne suffit pas).
# google-auth>=2.30
# google-auth-oauthlib>=1.2
# google-api-python-client>=2.130
# Le Phare — Génération PDF des rapports clients (optionnel ; sans ces
# paquets, le rapport est produit en HTML imprimable depuis le navigateur)
# weasyprint>=60          # rendu fidèle HTML/CSS → PDF
# reportlab>=4.0          # alternative basée layout (utilisé par bulletin_pdf.py)
