# Dépendances pour le serveur HTTP en prod (Docker / Coolify).
# Plus light que requirements.txt qui contient les deps desktop (Tkinter/Pillow).

# --- Serveur HTTP ---
fastapi==0.115.6
starlette==0.41.3
uvicorn>=0.32.0,<0.40
bcrypt>=4.0
itsdangerous>=2.2
python-dotenv>=1.0
python-multipart>=0.0.9

# --- Backend Triskell ---
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
supabase>=2.4
anthropic>=0.40
claude-agent-sdk>=0.1.0
chevron>=0.14   # Mustache renderer pour le builder Pixel Pros

# --- Parsers fichiers (Convoi, etc.) ---
Pillow>=10.0
openpyxl>=3.1
python-docx>=1.1
pypdf>=4.0

# --- Web Push notifications (mobile/desktop) ---
pywebpush>=2.0
