# Dépendances pour le serveur HTTP en prod (Docker / Coolify).
# Plus light que requirements.txt qui contient les deps desktop (Tkinter/Pillow).

# --- Serveur HTTP ---
fastapi==0.115.6
starlette==0.41.3
uvicorn>=0.32.0,<0.40
bcrypt>=4.0
itsdangerous>=2.2
python-dotenv>=1.0
python-multipart>=0.0.9

# --- Backend Triskell ---
requests>=2.31
httpx>=0.27                     # Argus — client HTTP async (scrapers)
beautifulsoup4>=4.12
lxml>=5.0
dnspython>=2.4                  # Vérif MX : ne garder que les mails dont le domaine reçoit du courrier (anti-rebond, protège la réputation d'envoi)
supabase>=2.4
anthropic>=0.40
claude-agent-sdk>=0.1.0
chevron>=0.14   # Mustache renderer pour le builder Pixel Pros
google-api-python-client>=2.0   # Chasseur Créateur — recherche YouTube
langdetect>=1.0.9               # Filtre langue française (Chasseur Créateur)
playwright>=1.48                # Argus — Chromium headless pour Pages Jaunes/Europages

# --- Parsers fichiers (Convoi, etc.) ---
Pillow>=10.0
openpyxl>=3.1
python-docx>=1.1
pypdf>=4.0
reportlab>=4.0   # Génération PDF côté serveur (export Obelisk + bulletins Phare)

# --- Web Push notifications (mobile/desktop) ---
pywebpush>=2.0

# --- Voix de Perceval (synthèse neurale Microsoft, gratuite, sans clé) ---
edge-tts>=6.1
